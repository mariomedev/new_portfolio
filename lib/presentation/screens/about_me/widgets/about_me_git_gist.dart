import 'package:flutter/material.dart';

class AboutMeGitGist extends StatelessWidget {
  const AboutMeGitGist({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(),
    );
  }
}
